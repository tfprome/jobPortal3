services:
  mongodb:
    image: mongo:7

    ports:
      - "8082:27017"

    volumes:
      - mongodb_data:/data/db

    networks:
      - react-job-portal-network

volumes:
  mongodb_data:

networks:
  react-job-portal-network:
    external: true
